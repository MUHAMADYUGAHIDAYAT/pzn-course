const nama = ["Muhamad", "Yuga", "Hidayat"];
for (const lesu of nama){
    document.writeln(`<p>${lesu}</p>`)
}

const namaLengkap = "Muhamad Yuga Hidayat";
for(const lesu2 of namaLengkap){
    document.writeln(`<p>${lesu2}</p>`);
}